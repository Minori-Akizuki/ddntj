import Vue from 'vue';
import Router from 'vue-router';

Vue.use(Router);

// 現在非利用。念のため作成。部屋機能作ったら使うかも
export default new Router ({
  routes: [
    {
      path: '/',
      name: '',
      component: app
    }
  ]
});
